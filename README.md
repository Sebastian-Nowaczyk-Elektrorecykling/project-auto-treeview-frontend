this is a frontend of a web app that views and edits tree-structured data
it uses project-auto-treeview-contract as a spec to contact the backend
the spec is implemented by project-auto-treeview-backend
